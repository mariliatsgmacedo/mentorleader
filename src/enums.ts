export enum UserRole {
    ADMIN = 'ADMIN',
    USER = 'USER',
    MENTOR = 'MENTOR',
}

export enum Local {
    USER_AUTH = '@UBBWW'
}

export enum Tables {
    USERS = 'users',
    SKILLS = 'skills',
    USER_SKILLS = 'userskills'
}
